# Project Roadmap & Next Steps

This file lists prioritized next tasks for the Document-Scanner project with short descriptions and acceptance criteria.

## Immediate (0-2 days)
- Pin dependency versions in `requirements.txt` — create reproducible installs.  
  - Acceptance: `requirements.txt` contains exact versions and `pip install -r requirements.txt` reproduces environment.
- Finalize Qt migration of desktop UI (`gui_qt.py`) and deprecate tkinter usage in `gui_new.py`.  
  - Acceptance: Desktop app launches using Qt by default; tkinter imports are optional/guarded.
- Add automated tests (pytest) and a minimal E2E harness for the web UI.  
  - Acceptance: `pytest` runs unit tests and a simple request test against the Flask app.
- Polish web UI accessibility and UX (copy/export buttons already added).  
  - Acceptance: keyboard-accessible controls and visible focus states; export/download works.

## Short-term (1-2 weeks)
- Add continuous integration (GitHub Actions) to run tests and lint on PRs.  
  - Acceptance: Actions.yml runs tests on pushes/PRs and reports status.
- Improve documentation: README examples, screenshots, and a Quick Start section.  
  - Acceptance: README includes screenshots and minimal usage guide.
- Package the app: provide a `setup.py`/`pyproject.toml` or a wheel build plus packaging instructions.  
  - Acceptance: `python -m build` produces a wheel.

## Medium-term (2-6 weeks)
- Performance profiling and optimization for large documents (memory/CPU).  
  - Acceptance: Profiling report + at least one targeted optimization with measurable improvement.
- Add persistent user settings (last-used folders, UI prefs) and export/import settings.  
  - Acceptance: Settings saved/loaded and exported to JSON.
- Improve test coverage and add regression tests for core algorithms.  
  - Acceptance: Coverage increases and tests prevent regressions.

## Long-term
- Cloud integration for batch scanning (S3/Google Drive connectors).  
- Corpus analytics and visualization for multi-file similarity heatmaps.  
- User studies and iterative UI tweaks based on feedback.

## How to contribute
- Pick an item above and open a PR with focused changes.  
- Run tests locally before pushing: `pip install -r requirements.txt && pytest`.

---
Generated by the assistant to track next steps and priorities.
